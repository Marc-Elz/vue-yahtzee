Issue 4: Css van tables kan beter met minimale veranderingen

Issue 3: Dobbelstenen op het begin leeg laten. N/A

Jasper end of day tip:
Object.entries word vaak herhaald. Ik moet kijken of ik dat kan vermijden. 

Als ik er niet zelf uitkom, tip om datde volgende keer maar vragen


Voor de werkweek issues:

Issue 1:
Ik gebruik Object.entries voor alle problemen zelfs als ik keys niet nodig heb. ipv kan ik .values of .keys gebruiken waarnodig

Issue 2: 
Hoe variabelen aan computed properties geven?
const computedFunction = computed(() => (var) => {})
const computedProp = computed(() =>{})

Ik snap denk ik niet goed genoeg, waarom moeten deze functies computed zijn. Maar in welk geval hoeft het niet computed te zijn?
> Ik denk dat ik het nu met getTotalCountdown beter begrijp. aangezien je .value op die properities kan doen maar niet op functies. Door .value te refereren voorkom je dat de functie opnieuw moet runnen.

Ik moet beter opletten dat ik niet de reference van een property zelf return. maar de reference.value


Wat ik moet doen:
1. object.entries vervangen door directere maniereen om de functies op te lossen.
2. Vraag aan IT begeleider uitleg wanneer values computed moeten zijn. En opheldering waarom. Kan ik beter functies als handleStraight 
aanpassen naar 2 duplicate functies, 
of computed property maken met parameters, 
of gewoon als functies.
Ik wil de tradeoffs begrijpen.


Learnings
Computed properties kunnen geen properties hebben, maar ze kunnen wel functies aanroepen.

Ik gebruik object.values en object.entries wanneer ik gewoon een for loop kan gebruiken. Waarom ik dat deed: het werkte bij een functie dus herapplyde ik diezelfde structuur bij meerdere
ook al heb ik in het verleden natuurlijk met for loops in javascript geleerd, en de voorbeelden die gegeven waren gebruikten for loops. Ik had dit basisconcept niet voor ogen.

Maak gebruik van het dice object wanneer die structuur beter past. De reden waarom ik dat deed was omdat ik de feedback die ik kreeg om het count object te gebruiken applyde op
alle functies, die regel heb ik zelf in mijn hoofd gebhouden, Maar: denk voor jezelf na of het een goed idee is